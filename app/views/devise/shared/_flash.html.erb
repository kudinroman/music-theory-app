<% flash.each do |type, message| %>
  <div class="max-w-7xl mx-auto px-4 py-2 text-center
              <%= type == 'alert' ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700' %> rounded mb-4">
    <%= message %>
  </div>
<% end %>
