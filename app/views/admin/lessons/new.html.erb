<h1 class="text-2xl font-bold mb-4">New Lesson for "<%= @block.title %>"</h1>

<%= render "form", lesson: @lesson %>

<%= link_to "Back", admin_block_lessons_path(@block), class: "text-blue-600 mt-4 inline-block" %>
